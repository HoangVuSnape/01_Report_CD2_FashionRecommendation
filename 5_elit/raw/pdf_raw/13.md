1.3 Weekly Progress Report- Week 13
Date range: 23/03- 29/03
Tasks completed
• Trong quá trình nghiên cứu sâu về các mô hình GNN cho hệ thống đề xuất thời
trang, em tình cờ phát hiện một repo rất liên quan do hai bạn sinh viên đã làm
việc cùng thầy thực hiện: anh Trần Gia Ưu (52100946) và anh Nguyễn Khắc
13
Anh Tài (52100306). Dự án Fashion Product Recommendation System sử
dụng LightGCN kết hợp feature extraction từ hình ảnh (MobileNetV2) và văn
bản (BERT), rất phù hợp với hướng nghiên cứu của chúng ta.
• Đọchiểusourcecodechi tiết, phân tích cấu trúc pipeline từ dữ liệu thô (hình ảnh
.jpg, văn bản mô tả) → preprocessing (YOLO detection, feature extraction) →
xây dựng adjacency matrix (.npz) → LightGCN training → evaluation metrics
(Recall, NDCG).
• Phân tích các biến thể mô hình trong repo: onlyimg (chỉ dùng CNN embed
dings), bertimg (hybrid CNN+BERT), tfidfbert (TF-IDF văn bản), và các kỹ
thuật sampling như vcr1p, vcr5p cho dataset Vibrent/H&M [file:38][file:31].
• Thử nghiệm chạy local một số module chính (rsimgapp cho visual similarity,
hrssystem cho hybrid recommendation) và đánh giá hiệu suất trên dataset mẫu.
• Lập kế hoạch refact và customize lại pipeline để tích hợp vào hệ thống Fashion
Recommender VTO của chúng ta, tập trung vào việc kết hợp với PostgreSQL
schema và microservices architecture đã thiết kế ở tuần trước.
Current status
Repo của anh Tài và anh Ưu cung cấp một pipeline hoàn chỉnh và thực tế cho hệ thống
đề xuất thời trang dựa trên GNN, với hai module chính rất giá trị:
• rsimgapp:Tìmkiếmsảnphẩmtươngtựbằnghìnhảnh(YOLO→MobileNetV2
→FAISS vector search), phù hợp cho tính năng “tìm outfit tương tự”.
• hrssystem:Hybridrecommendationkếthợpuser-iteminteraction, NLP(BERT),
và visual features qua LightGCN/CombiGCN, rất gần với kiến trúc chúng ta
đang hướng tới [file:31][file:35].
Em đã hiểu rõ cách họ xử lý dữ liệu lớn (precompute .npz adjacency matrices thay vì
load raw images realtime) và các biến thể mô hình (onlyimg vs. bertimg), giúp tránh
14
các bottleneck về memory và I/O. Hiện tại em đang chuẩn bị refact để tích hợp vào
Docker Compose và PostgreSQL backend của dự án.
Issues and blockers
• Pipeline gốc dùng .npz files cho adjacency matrices, cần chuyển đổi để tương
thích với PostgreSQL/pgvector (lưu embeddings vector và similarity search trực
tiếp trong DB).
• Chạy full training trên dataset lớn (Vibrent full) gặp vấn đề GPU memory, cần
optimize batch size và sampling (vcr1p/vcr5p đã giúp giảm tải nhưng vẫn cần
kiểm tra thêm).
• Cần thời gian để customize các module (rsimgapp, hrssystem) thành microser
vices riêng biệt, phù hợp với architecture Core API + AI Service của dự án.
1.3.1 note
Tuần 13 đánh dấu bước tiến quan trọng khi em tìm được repo thực tế từ anh Tài và
anh Ưu, giúp rút ngắn đáng kể thời gian phát triển pipeline GNN recommendation.
Việc customize thành microservices sẽ giúp hệ thống Fashion Recommender VTO có
một recommendation engine mạnh mẽ, kết hợp visual similarity (cho try-on/outfit) và
semantic matching (cho mô tả sản phẩm), rất phù hợp với nhóm người dùng giới trẻ.